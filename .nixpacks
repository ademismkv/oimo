[phases.setup]
nixPkgs = ['python39', 'python39Packages.pip']

[phases.install]
cmds = ['cd core-api && pip install -r requirements.txt']

[start]
cmd = 'cd core-api && uvicorn main:app --host 0.0.0.0 --port $PORT' 